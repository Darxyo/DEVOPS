events {
    worker_connections 1024;
}

http {
    upstream ping-api {
        least_conn;
        server ping-api:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://ping-api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}